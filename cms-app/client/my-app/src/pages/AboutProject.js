import React from 'react';

const AboutProject = () => {
    return (
        <div>
            <div className="container mt-5 card">
                <div className="card-header">
                    <h4>About Product</h4>
                </div>
                <div className="card-body">
                    This product is basically to perform the CRUD (Create, Read, Update and Delete) operations with the
                    help of Client side application built with react js Library and Node.js at the backend with the Rest
                    APis.
                </div>
            </div>
        </div>
    );
};

export default AboutProject;